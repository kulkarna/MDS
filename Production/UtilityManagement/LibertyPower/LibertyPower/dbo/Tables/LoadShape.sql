CREATE TABLE [dbo].[LoadShape] (
    [ID]            INT          IDENTITY (1, 1) NOT NULL,
    [LoadShapeCode] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_LoadShape] PRIMARY KEY CLUSTERED ([ID] ASC)
);

