CREATE TABLE [dbo].[pricing_file_type] (
    [file_type_id]     INT          IDENTITY (1, 1) NOT NULL,
    [file_type]        VARCHAR (50) NOT NULL,
    [file_type_abbrev] VARCHAR (50) NOT NULL
);

